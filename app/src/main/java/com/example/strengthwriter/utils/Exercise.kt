package com.example.strengthwriter.utils

enum class Exercise(
    id: Int,
    name: String
) {
    SHOULDER_PRESS(1, "SHOULDER PRESS"),
    DEAD_LIFT(2, "DEAD LIFT"),
    BACK_SQUAT(3, "BACK SQUAT"),
    FRONT_SQUAT(3, "FRONT SQUAT")
}
